# Test provider for PHP-magpie framework

- Based on PHPUnit 10.x and above.
- Describe the test suite using `phpunit.xml` in the root folder of your project.
- Run test using the command `php mp test`.
